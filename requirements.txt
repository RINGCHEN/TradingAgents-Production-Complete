# TradingAgents Production Dependencies
# This file is optimized for a small footprint production environment.

# ============= PyTorch CPU-Only Source =============
# Ensures that only the lightweight CPU versions of torch and torchvision are installed.
--extra-index-url https://download.pytorch.org/whl/cpu

# ============= 升級核心工具 =============
pip>=23.0
setuptools>=68.0
wheel>=0.40.0

# ============= 核心Web框架 =============
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
starlette==0.27.0

# ============= 配置安全驗證 (GOOGLE建議) =============
pydantic[email]==2.5.0

# ============= 圖片處理 =============
Pillow==10.0.1

# ============= 資料庫 =============
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1
asyncpg==0.29.0

# ============= Redis緩存 (97.5%性能提升) =============
redis[hiredis]==5.0.1

# ============= 認證和安全 =============
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-oauth2==1.1.1
PyJWT==2.8.0

# ============= HTTP客戶端 =============
httpx==0.25.2
aiohttp==3.9.1
requests==2.31.0
urllib3>=2.5.0

# ============= 工具套件 =============
python-dotenv==1.0.0
email-validator==2.1.0
python-dateutil==2.8.2
aiofiles==23.2.1
asyncio-throttle==1.0.2

# ============= AI API =============
openai==1.12.0
anthropic==0.18.1

# ============= AI / Data Processing (CPU Optimized) =============
# torch and torchvision are specified as CPU-only to minimize the application size.
torch==2.0.1+cpu
torchvision==0.15.2+cpu
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.3.0
scipy==1.13.0
xgboost==2.0.3  # 修復：基於GOOGLE診斷，添加缺失的核心依賴
statsmodels==0.14.2  # 修復：基於GOOGLE清剿指令，添加深度行為學習系統依賴

# ============= 金融數據 =============
yfinance==0.2.28

# ============= 緩存 =============
redis==5.0.1

# ============= WebSocket =============
websockets==12.0

# ============= 監控和日誌 =============
structlog==23.2.0
prometheus-client==0.19.0
psutil==5.9.8

# ============= 其他工具 =============
click==8.1.7
rich==13.7.0
beautifulsoup4==4.12.2
PyYAML==6.0.1

# ============= Cloud Storage for Models =============
boto3==1.34.57

# ============= 生產部署 =============
gunicorn==21.2.0